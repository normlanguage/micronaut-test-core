package micronaut.test.core

import micronaut.test.core.annotation.MockBean

@MockBean()
class TestGreeting {
  String message() {
    return "test"
  }
}

Void main() {
  printLine(TestGreeting().message())
}
